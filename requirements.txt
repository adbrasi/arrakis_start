# Requirements for Arrakis Start v2.0
# Install with: pip install -r requirements.txt
#
# These dependencies run inside the orchestrator venv (not the ComfyUI venv).
# In cloud deploys the bootstrap.sh script keeps huggingface_hub/hf_xet in
# sync, but listing them here lets the project be installed manually from
# this file alone.

# WebSocket server for real-time updates
websockets>=12.0

# Process management and port introspection
psutil>=5.9.0

# Small HTTP helper used by downloader/process_manager
requests>=2.31.0

# HuggingFace downloads (CLI + Xet backend).
# hf_xet is pulled in automatically by huggingface_hub >=0.32 but we pin
# it explicitly so pip installs cover the download path even when the
# hub extras aren't resolved first.
huggingface_hub[cli]>=1.3.0,<2.0
hf_xet>=1.0.0
